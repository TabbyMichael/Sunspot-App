# App Icon

Place your app icon image (PNG format, 1024x1024 pixels recommended) here as `app_icon.png`.

After adding the icon, run:
```bash
flutter pub get
flutter pub run flutter_launcher_icons
```

This will generate the app icons for all platforms (Android, iOS, Web, Windows, macOS).
